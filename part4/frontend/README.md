
  # Hbnb clone project

  This is a code bundle for Hbnb clone project. The original project is available at https://www.figma.com/design/8ySdTz5L4dndDpfKSfLQHR/Hbnb-clone-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  